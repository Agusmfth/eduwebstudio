@extends('layouts.admin') @section('title','Pesan Konsultasi') @section('content')<div class="panel"><div class="table-wrap"><table><thead><tr><th>Waktu</th><th>Pengirim</th><th>Kontak</th><th>Pesan</th><th>Aksi</th></tr></thead><tbody>@forelse($inquiries as $row)<tr><td>{{ $row->created_at->format('d M Y H:i') }}<br><span class="badge {{ $row->is_read?'off':'' }}">{{ $row->is_read?'Dibaca':'Baru' }}</span></td><td><strong>{{ $row->name }}</strong><br>{{ $row->school }}</td><td><a href="https://wa.me/{{ preg_replace('/\D/','',$row->phone) }}" target="_blank">{{ $row->phone }}</a><br>{{ $row->email }}</td><td style="min-width:260px">{{ $row->message }}</td><td><div class="actions">@unless($row->is_read)<form method="post" action="{{ route('admin.inquiries.read',$row) }}">@csrf @method('patch')<button class="btn btn-light">Tandai Dibaca</button></form>@endunless<form method="post" action="{{ route('admin.inquiries.destroy',$row) }}" onsubmit="return confirm('Hapus pesan ini?')">@csrf @method('delete')<button class="btn btn-danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="5">Belum ada pesan.</td></tr>@endforelse</tbody></table></div>{{ $inquiries->links() }}</div>@endsection
